{
  "enabled": true,

  "input_entity" : {
    "source": {
      "type": "csv",
      "file_name": "",
      "read_options": { "header": true, "delimiter": ",", "encoding": "UTF-8" },
      "schema": {
        "columns": [
          { "target": "order_id", "type": "long", "nullable": false },
          { "target": "customer_id", "type": "string", "nullable": true},
          { "target": "order_amount", "type": "decimal(12,2)", "nullable": true },
          { "target": "order_date", "type": "date", "nullable": true, "transform_sql": "to_date(order_date)" }
        ]
      }
    }
  },

  "target_entity": {

  },

  "ingestion_information": {
    "mode": "append"
  }

}
